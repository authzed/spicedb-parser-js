# spicedb-parser-js

This library contains shared parser logic common to the Playground and the VSCode extension.

It's currently tightly tied to the implementations of those packages. We'd welcome suggestions on how to make it more broadly useful.
